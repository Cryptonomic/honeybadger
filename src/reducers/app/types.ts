export interface State {
    publicKey: string;
    secretKey: string;
    publicKeyHash: string;
    storeType: string;
    seed: string;
    balance: number;
    sendStep: number;
    sendAddress: string;
}
